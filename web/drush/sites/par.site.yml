dev:
  user: 1
  root: /var/www/par/web
  command:
    sql:
      dump:
        options:
          ordered-dump: true
          structure-tables-key: 'common'
          skip-tables-key: 'common'
          extra: '-O -x -w'
      sanitize:
        options:
          sanitize-password: 'TestPassword'
          sanitize-email: 'no'
paas:
  user: 1
  command:
    sql:
      dump:
        options:
          ordered-dump: true
          structure-tables-key: 'common'
          skip-tables-key: 'common'
          extra: '-O -x -w'
      sanitize:
        options:
          sanitize-password: 'TestPassword'
          sanitize-email: 'no'
