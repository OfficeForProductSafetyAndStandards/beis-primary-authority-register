services:
  par_data.commands:
    class: \Drupal\par_data\Commands\ParDataCommands
    arguments:
      - '@par_data.manager'
      - '@entity_type.manager'
    tags:
      - { name: drush.command }
