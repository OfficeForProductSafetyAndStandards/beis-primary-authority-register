services:
  par_cache.commands:
    class: \Drupal\par_cache\Commands\ParCacheCommands
    arguments: ['@par_cache.persistent.cache_factory.inner', '%cache_lifecycle_bins%']
    tags:
      - { name: drush.command }

