<?php

/**
 * @file
 * Contains par_member_cease_flows.module.
 */
use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function par_enquiry_send_flows_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the par_enquiry_send_flows module.
    case 'help.page.par_enquiry_send_flows':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Allows enforcement officers to submit a general enquiry to the PA.') . '</p>';
      return $output;

    default:
  }
}
