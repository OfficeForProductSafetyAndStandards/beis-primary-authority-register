<?php

/**
 * @file
 * Contains par_enforcement_raise_flows.module.
 */
use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function par_enforcement_raise_flows_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the par_enforcement_raise_flows module.
    case 'help.page.par_enforcement_raise_flows':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Allows enforcement officers to raise an enforcement against an organisation.') . '</p>';
      return $output;

    default:
  }
}
