<?php

/**
 * @file
 * Contains par_member_cease_flows.module.
 */
use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function par_deviation_request_flows_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the par_deviation_request_flows module.
    case 'help.page.par_deviation_request_flows':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Allows enforcement officers to deviate from the normal inspection plan procedure.') . '</p>';
      return $output;

    default:
  }
}
