config:
  target: 'https://par-beta-staging.cloudapps.digital'
  phases:
    - duration: 10
      arrivalRate: 10
  defaults:
    headers:
      Accept: '*/*'

scenarios:
  - flow:
    - get:
        url: "/"
