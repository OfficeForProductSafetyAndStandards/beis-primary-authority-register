diff --git a/.docksal/services/maven/pom.xml b/.docksal/services/maven/pom.xml
index d4d3d16f9..c4f2ca843 100644
--- a/.docksal/services/maven/pom.xml
+++ b/.docksal/services/maven/pom.xml
@@ -2,15 +2,15 @@
 	<modelVersion>4.0.0</modelVersion>
 	<groupId>uk.gov.beis</groupId>
 	<artifactId>par</artifactId>
-	<version>0.0.1-SNAPSHOT</version>
+	<version>0.0.1-SNAPSHOT-Dockal</version>
 	<name>par-test-automation</name>
-	<description>Automation regression testing for PAR</description>
+  <description>Automation regression testing for PAR running under Docksal</description>

 	<properties>
 		<slf4j.version>1.7.25</slf4j.version>
 		<joda-time.version>2.3</joda-time.version>

-		<logback.version>1.2.3</logback.version>
+		<logback.version>1.5.18</logback.version>
 		<junit.version>4.13.2</junit.version>

 		<!-- <selenium-java.version>2.53.1</selenium-java.version> -->
@@ -19,15 +19,21 @@
 		<selenium-chrome-java-version>4.12.1</selenium-chrome-java-version>
 		<selenium-ie-java.version>3.14.0</selenium-ie-java.version>

-		<google-code-gson.version>2.8.5</google-code-gson.version>
+		<google-code-gson.version>2.13.0</google-code-gson.version>
 		<cluecumber.report.version>2.5.0</cluecumber.report.version>

 		<io-cucumber-java.version>7.21.1</io-cucumber-java.version>
 		<io-cucumber-core.version>7.21.1</io-cucumber-core.version>
-		<io-cucumber-junit.version>7.21.1</io-cucumber-junit.version>
+		<io-cucumber-junit.version>7.22.0</io-cucumber-junit.version>

 		<maven.failsafe.plugin.version>3.0.0-M5</maven.failsafe.plugin.version>
 		<maven.compiler.plugin.version>3.2</maven.compiler.plugin.version>
+
+    <!-- @todo fix these hardcoded paths and hence the need for defining the plugin -->
+    <project.resources>/resources</project.resources>
+    <project.testResources>/app/src/test/resources</project.testResources>
+    <maven.resources.plugin.version>3.3.1</maven.resources.plugin.version>
+
 	</properties>

 	<dependencies>
@@ -119,6 +125,10 @@
 	</dependency>
 	</dependencies>
 	<build>
+    <!-- @todo fix these hardcoded paths -->
+    <sourceDirectory>/app/src/main</sourceDirectory>
+    <testSourceDirectory>/app/src/test</testSourceDirectory>
+
 		<plugins>
 			<plugin>
 				<groupId>org.apache.maven.plugins</groupId>
@@ -130,6 +140,11 @@
 						<include>PARTestRunner.java</include>
 					</includes>
 					<testFailureIgnore>true</testFailureIgnore>
+          <systemPropertyVariables>
+            <env>docksal</env>
+            <!-- @todo fix this hardcoded path -->
+            <basedir>/app</basedir>
+          </systemPropertyVariables>
 				</configuration>
 			</plugin>
 			<plugin>
@@ -141,6 +156,23 @@
 					<target>1.8</target>
 				</configuration>
 			</plugin>
+      <plugin>
+        <groupId>org.apache.maven.plugins</groupId>
+        <artifactId>maven-resources-plugin</artifactId>
+        <version>${maven.resources.plugin.version}</version>
+        <configuration>
+          <resources>
+            <resource>
+              <directory>${project.resources}</directory>
+              <filtering>true</filtering>
+            </resource>
+            <resource>
+              <directory>${project.testResources}</directory>
+              <filtering>true</filtering>
+            </resource>
+          </resources>
+        </configuration>
+      </plugin>
 			<plugin>
 				<groupId>com.trivago.rta</groupId>
 				<artifactId>cluecumber-report-plugin</artifactId>
